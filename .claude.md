# Claude AI 项目记忆

## Jackson 配置

**文件：** `ai-studio-service/src/main/resources/application.yml`

**配置：** `property-naming-strategy: SNAKE_CASE`

**影响：** Jackson 反序列化时期望接收 snake_case 格式字段名（如 `old_password`）。如果前端发送 camelCase 格式（如 `oldPassword`），会导致字段无法映射，接收值为 `null`。

---

## 问题：修改密码接口参数验证失败

**现象：**
- 前端发送：`{ "oldPassword": "xxx", "newPassword": "xxx" }`
- 后端接收：`{ "oldPassword": null, "newPassword": null }`
- 错误：`"旧密码不能为空; 新密码不能为空"`

**原因：** Jackson SNAKE_CASE 策略期望 `old_password`、`new_password`

**解决：** 在 DTO 字段上使用 `@JsonProperty` 注解明确指定字段名

```java
@JsonProperty("oldPassword")
private String oldPassword;

@JsonProperty("newPassword")
private String newPassword;
```

**注意：** 小写字段（如 `email`、`avatar`）不受影响，无需注解
