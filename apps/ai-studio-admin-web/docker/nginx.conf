server {
    listen 80;
    server_name _;

    location /ai-studio/v1/ {
        proxy_pass http://ai-studio-service:8080/ai-studio/v1/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = / {
        return 302 $scheme://$http_host/ai-studio-plus-admin-web/;
    }

    location /ai-studio-plus-admin-web/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /ai-studio-plus-admin-web/index.html;
    }
}
